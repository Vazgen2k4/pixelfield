<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <!-- Background color of the splash screen -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Theme to apply after the splash screen is removed -->
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
        <!-- Optional: Disable force dark mode -->
        <!-- Optional: Make the splash screen fullscreen -->
        <!-- Optional: Control system bar visibility -->
        <!-- Optional: Handle display cutouts (notches) -->
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Normal theme applied after the splash screen is removed -->
    <style name="NormalTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Background color of the app -->
        <item name="android:windowBackground">?android:colorBackground</item>
        <!-- Optional: Customize other theme attributes -->
    </style>
</resources>
